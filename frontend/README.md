# College ERP Frontend

React + Vite frontend for the College ERP.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` if the Spring Boot backend is not at `http://localhost:8080/api`.

Build:

```bash
npm run build
```
